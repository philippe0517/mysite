# 反馈管理台使用指南

> 管理台已上线：**https://philippe0517.github.io/mysite/admin.html**
> 代码部分已完成，以下是你需要手动操作的步骤（账号操作由你本人负责）。

---

## 第一步：创建管理员账号（约 2 分钟）

管理台用 Supabase Auth 登录，账号不是注册的，是你在后台手动创建的：

1. 打开 https://supabase.com 登录，进入你的项目（mysite-feedback）
2. 左侧菜单点击 **Authentication**（盾牌图标）
3. 点击 **Users** → 右上角 **Add user** → **Create new user**
4. 填写：
   - **Email**：你的邮箱（建议用**不含手机号**的邮箱）
   - **Password**：强密码（只属于这个管理台，不要与其他密码相同）
   - **Auto Confirm User**：勾选 ✅（跳过邮箱验证）
5. 点击 **Create user**

## 第二步：开启实时通知（可选，约 1 分钟）

想让新反馈**实时弹出提示**，需要把 feedback 表加入 Realtime：

1. 左侧菜单点击 **Database**（数据库图标）
2. 找到 **Publications**（或 Replication）→ 点击 `supabase_realtime`
3. 把 `feedback` 表的开关打开 → 保存

没开启也没关系：管理台其他功能全部正常，只是顶部状态点显示黄色「实时未连接」。

## 第三步：登录使用

1. 打开 https://philippe0517.github.io/mysite/admin.html
2. 输入第一步创建的邮箱和密码 → 登录

### 你能做什么

| 功能 | 说明 |
|---|---|
| **反馈列表** | 最新 300 条，时间倒序，显示昵称/关系/设备/版本/时间 |
| **搜索筛选** | 按关键字搜内容，按设备/版本筛选 |
| **统计卡片** | 筛选结果数、今日新增、设备分布 Top3 |
| **删除** | 删除不当反馈（有确认提示，不可恢复） |
| **导出 CSV** | 一键导出当前筛选结果（带 BOM，Excel 打开中文不乱码）——**下节课"整理反馈、判断采纳"就用它** |
| **实时通知** | 开启第二步后，新反馈到达时顶部弹提示，列表自动插入并高亮 |

## 安全设计说明（对照课件）

- **管理台页面是公开的**——这没关系！因为安全在数据库层（RLS）：
  - 未登录访客调用 API 读取反馈 → 被数据库以 `42501 权限不足` 拒绝（已实测验证）
  - 管理员能读写，靠的是**登录后的 authenticated 角色** + 你在 SQL 里写好的策略
  - 正是课件那句话：**「页面隐藏不等于安全，安全在数据库层」**
- 管理台前端只有 publishable key（公开密钥），**没有也不需要** service_role key
- 忘记密码：Supabase → Authentication → Users → 选中账号 → 重置/删除重建

## 常见问题

**Q：登录提示 Invalid login credentials？**
账号密码错误，或账号还没创建（先做第一步）。

**Q：列表读取失败？**
说明登录会话过期或 RLS 策略异常，刷新页面重新登录。

**Q：实时状态点一直黄色？**
第二步没做，或 Supabase 免费版 Realtime 休眠了——刷新页面即可，不影响其他功能。
